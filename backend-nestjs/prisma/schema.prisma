// ═══════════════════════════════════════════════════════════════════
// PHASE-1B.2-SCHEMA-V1  (checkout, payment intents, ledger)
//
// لو السطر ده مش موجود في أول prisma/schema.prisma عندك، يبقى الملف
// المطبَّق مش الملف ده.
//
// اتحقق منه بـ:
//     head -2 prisma/schema.prisma
//     grep -c "^model Payment" prisma/schema.prisma      → لازم 1
//
// الموديلات المؤقتة لجداول الدفع غير الموجودة في قاعدة البيانات اتشالت
// من هنا ومحفوظة في prisma/phase1b-placeholders.prisma.bak
// ═══════════════════════════════════════════════════════════════════

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model cache {
  key        String @id @db.VarChar(255)
  value      String
  expiration Int

  @@index([expiration], map: "cache_expiration_index")
}

model cache_locks {
  key        String @id @db.VarChar(255)
  owner      String @db.VarChar(255)
  expiration Int

  @@index([expiration], map: "cache_locks_expiration_index")
}

model device_verification_tokens {
  id          BigInt    @id @default(autoincrement())
  user_id     BigInt
  fingerprint String    @db.VarChar(255)
  token       String    @unique(map: "device_verification_tokens_token_unique") @db.VarChar(255)
  ip_address  String    @db.VarChar(255)
  user_agent  String
  expires_at  DateTime  @db.Timestamp(0)
  used_at     DateTime? @db.Timestamp(0)
  created_at  DateTime? @db.Timestamp(0)
  updated_at  DateTime? @db.Timestamp(0)
  users       users     @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "device_verification_tokens_user_id_foreign")
}

model device_verifications {
  id           BigInt    @id @default(autoincrement())
  user_id      BigInt
  fingerprint  String    @db.VarChar(255)
  ip_address   String?   @db.VarChar(255)
  user_agent   String?   @db.VarChar(255)
  device_name  String?   @db.VarChar(255)
  platform     String?   @db.VarChar(255)
  browser      String?   @db.VarChar(255)
  is_verified  Boolean   @default(false)
  verified_at  DateTime? @db.Timestamp(0)
  last_used_at DateTime? @db.Timestamp(0)
  created_at   DateTime? @db.Timestamp(0)
  updated_at   DateTime? @db.Timestamp(0)
  code         String
  users        users     @relation(fields: [user_id], references: [id])

  @@unique([user_id, fingerprint], map: "device_verifications_user_id_fingerprint_unique")
  @@index([fingerprint], map: "device_verifications_fingerprint_index")
}

model devices {
  id                      BigInt    @id @default(autoincrement())
  user_id                 BigInt
  fingerprint             String?   @db.VarChar(255)
  browser                 String?   @db.VarChar(255)
  ip_address              String?   @db.VarChar(255)
  verification_token      String?   @db.VarChar(255)
  verified_at             DateTime? @db.Timestamp(0)
  created_at              DateTime? @db.Timestamp(0)
  updated_at              DateTime? @db.Timestamp(0)
  last_active_at          DateTime? @db.Timestamp(0)
  os                      String?   @db.VarChar(255)
  trust_token             String?   @db.VarChar(64)
  session_id              String?   @db.VarChar(255)
  logged_out_at           DateTime? @db.Timestamp(0)
  platform                String?   @db.VarChar(255)
  hardware_signature      String?
  device_name             String?   @db.VarChar(255)
  verification_expires_at DateTime? @db.Timestamp(0)
  users                   users     @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "devices_user_id_foreign")

  @@index([fingerprint], map: "devices_fingerprint_index")
  @@index([session_id], map: "devices_session_id_index")
}

model email_verifications {
  id         BigInt    @id @default(autoincrement())
  email      String    @db.VarChar(255)
  token      String    @db.VarChar(255)
  created_at DateTime? @db.Timestamp(0)

  @@index([email], map: "email_verifications_email_index")
}

model failed_jobs {
  id         BigInt   @id @default(autoincrement())
  uuid       String   @unique(map: "failed_jobs_uuid_unique") @db.VarChar(255)
  connection String
  queue      String
  payload    String
  exception  String
  failed_at  DateTime @default(now()) @db.Timestamp(0)
}

model job_batches {
  id             String  @id @db.VarChar(255)
  name           String  @db.VarChar(255)
  total_jobs     Int
  pending_jobs   Int
  failed_jobs    Int
  failed_job_ids String
  options        String?
  cancelled_at   Int?
  created_at     Int
  finished_at    Int?
}

model jobs {
  id           BigInt @id @default(autoincrement())
  queue        String @db.VarChar(255)
  payload      String
  attempts     Int    @db.SmallInt
  reserved_at  Int?
  available_at Int
  created_at   Int

  @@index([queue], map: "jobs_queue_index")
}

model migrations {
  id        Int    @id @default(autoincrement())
  migration String @db.VarChar(255)
  batch     Int
}

model notifications {
  id         BigInt    @id @default(autoincrement())
  user_id    BigInt
  type       String    @default("system") @db.VarChar(255)
  title      String    @db.VarChar(255)
  message    String
  data       Json?     @db.Json
  read_at    DateTime? @db.Timestamp(0)
  created_at DateTime? @db.Timestamp(0)
  updated_at DateTime? @db.Timestamp(0)
  users      users     @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "notifications_user_id_foreign")

  @@index([type], map: "notifications_type_index")
  @@index([user_id, read_at], map: "notifications_user_id_read_at_index")
}

model password_reset_tokens {
  email      String    @id @db.VarChar(255)
  token      String    @db.VarChar(255)
  created_at DateTime? @db.Timestamp(0)
}

model personal_access_tokens {
  id             BigInt    @id @default(autoincrement())
  tokenable_type String    @db.VarChar(255)
  tokenable_id   BigInt
  name           String
  token          String    @unique(map: "personal_access_tokens_token_unique") @db.VarChar(64)
  abilities      String?
  last_used_at   DateTime? @db.Timestamp(0)
  expires_at     DateTime? @db.Timestamp(0)
  created_at     DateTime? @db.Timestamp(0)
  updated_at     DateTime? @db.Timestamp(0)

  @@index([expires_at], map: "personal_access_tokens_expires_at_index")
  @@index([tokenable_type, tokenable_id], map: "personal_access_tokens_tokenable_type_tokenable_id_index")
}

model push_subscriptions {
  id         BigInt    @id @default(autoincrement())
  user_id    BigInt
  endpoint   String
  public_key String
  auth_token String
  created_at DateTime? @db.Timestamp(0)
  updated_at DateTime? @db.Timestamp(0)
  users      users     @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "push_subscriptions_user_id_foreign")
}

model security_logs {
  id         BigInt    @id @default(autoincrement())
  user_id    BigInt?
  event      String    @db.VarChar(255)
  ip_address String    @db.Inet
  user_agent String?
  created_at DateTime? @db.Timestamp(0)
  updated_at DateTime? @db.Timestamp(0)
}

model sessions {
  id            String  @id @db.VarChar(255)
  user_id       BigInt?
  ip_address    String? @db.VarChar(45)
  user_agent    String?
  payload       String
  last_activity Int

  @@index([last_activity], map: "sessions_last_activity_index")
  @@index([user_id], map: "sessions_user_id_index")
}

model trusted_devices {
  id                 BigInt    @id @default(autoincrement())
  user_id            BigInt
  device_fingerprint String    @db.VarChar(255)
  ip_address         String?   @db.VarChar(255)
  user_agent         String?
  device_name        String?   @db.VarChar(255)
  last_login_at      DateTime  @default(now()) @db.Timestamp(0)
  created_at         DateTime? @db.Timestamp(0)
  updated_at         DateTime? @db.Timestamp(0)
  users              users     @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "trusted_devices_user_id_foreign")

  @@unique([user_id, device_fingerprint], map: "trusted_devices_user_id_device_fingerprint_unique")
  @@index([user_id, last_login_at], map: "trusted_devices_user_id_last_login_at_index")
}

model user_devices {
  id               BigInt    @id @default(autoincrement())
  user_id          BigInt
  device_token     String    @unique(map: "user_devices_device_token_unique") @db.Uuid
  fingerprint      String    @db.VarChar(255)
  name             String?   @db.VarChar(255)
  platform         String?   @db.VarChar(255)
  browser          String?   @db.VarChar(255)
  type             String?   @db.VarChar(255)
  browser_version  String?   @db.VarChar(255)
  ip_address       String    @db.VarChar(255)
  location         Json?     @db.Json
  is_trusted       Boolean   @default(false)
  verified_at      DateTime? @db.Timestamp(0)
  last_login_at    DateTime? @db.Timestamp(0)
  last_activity_at DateTime? @db.Timestamp(0)
  logged_out_at    DateTime? @db.Timestamp(0)
  created_at       DateTime? @db.Timestamp(0)
  updated_at       DateTime? @db.Timestamp(0)
  deleted_at       DateTime? @db.Timestamp(0)
  session_id       String?   @db.VarChar(255)
  users            users     @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "user_devices_user_id_foreign")

  @@unique([user_id, device_token], map: "user_devices_user_id_device_token_unique")
  @@index([fingerprint])
  @@index([fingerprint], map: "user_devices_fingerprint_index")
  @@index([user_id])
  @@index([user_id], map: "user_devices_user_id_index")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model users {
  id                            BigInt                       @id @default(autoincrement())
  fullname                      String?                      @db.VarChar(255)
  username                      String                       @unique(map: "users_username_unique") @db.VarChar(255)
  email                         String                       @unique(map: "users_email_unique") @db.VarChar(255)
  email_verified_at             DateTime?                    @db.Timestamp(0)
  password                      String                       @db.VarChar(255)
  country                       String?                      @db.VarChar(255)
  country_code                  String?                      @db.VarChar(10)
  mobile_code                   String?                      @db.VarChar(10)
  avatar                        String?                      @db.VarChar(255)
  business_name                 String?                      @db.VarChar(255)
  completed_trade               Int                          @default(0)
  rating                        Float                        @default(0) @db.Real
  is_verified                   Boolean                      @default(false)
  remember_token                String?                      @db.VarChar(100)
  created_at                    DateTime?                    @db.Timestamp(0)
  updated_at                    DateTime?                    @db.Timestamp(0)
  entity_type                   String?                      @db.Char(255)
  email_verification_token      String?                      @db.VarChar(64)
  email_verification_expires_at DateTime?                    @db.Timestamp(0)
  accounttype                   String                       @default("individual") @db.VarChar(255)
  email_otp                     String?                      @db.VarChar(255)
  email_otp_expires_at          DateTime?                    @db.Timestamp(0)
  email_otp_attempts            Int                          @default(0) @db.SmallInt
  email_otp_last_sent_at        DateTime?                    @db.Timestamp(0)
  account_locked_until          DateTime?                    @db.Timestamp(0)
  email_otp_blocked_until       DateTime?                    @db.Timestamptz(0)
  email_otp_resend_attempts     Int                          @default(0)
  two_factor_secret             String?
  two_factor_enabled            Boolean                      @default(false)
  two_factor_confirmed_at       DateTime?                    @db.Timestamp(0)
  session_id                    String?                      @db.VarChar(255)
  login_attempts                Int                          @default(0) @db.SmallInt
  login_locked_until            DateTime?                    @db.Timestamp(0)
  last_login_attempt_at         DateTime?                    @db.Timestamp(0)
  timezone                      String                       @default("UTC") @db.VarChar(255)
  last_activity_at              DateTime?                    @db.Timestamp(0)
  password_reset_expires_at     DateTime?                    @db.Timestamp(0)
  password_reset_code           String?                      @db.VarChar(255)
  device_verification_tokens    device_verification_tokens[]
  device_verifications          device_verifications[]
  devices                       devices[]
  notifications                 notifications[]
  oauth_providers               oauth_providers[]
  push_subscriptions            push_subscriptions[]
  store                         store[]
  trusted_devices               trusted_devices[]
  user_devices                  user_devices[]
  wallets                       wallets?

  @@index([accounttype], map: "idx_users_accounttype")
  @@index([email], map: "idx_users_email")
  @@index([email, email_verified_at], map: "idx_users_email_verified")
  @@index([username], map: "idx_users_username")
  @@index([session_id], map: "users_session_id_index")
}

model login_blocks {
  id            BigInt    @id @default(autoincrement())
  fingerprint   String    @unique
  attempts      Int       @default(0)
  blocked_until DateTime?
  created_at    DateTime  @default(now())
  updated_at    DateTime  @updatedAt
  ip_address    String?
}

model wallets {
  id         BigInt    @id @default(autoincrement())
  user_id    BigInt    @unique
  balance    Float     @default(0)
  currency   String    @default("USDDC") @db.VarChar(5)
  created_at DateTime? @db.Timestamp(0)
  updated_at DateTime? @db.Timestamp(0)
  users      users     @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "1")
}

model oauth_providers {
  id            BigInt    @id @default(autoincrement())
  user_id       BigInt
  provider      String    @db.VarChar(50)
  provider_id   String    @db.VarChar(255)
  access_token  String?
  refresh_token String?
  expires_at    DateTime? @db.Timestamp(0)
  profile_data  Json?     @db.Json
  created_at    DateTime? @db.Timestamp(0)
  updated_at    DateTime? @db.Timestamp(0)
  users         users     @relation(fields: [user_id], references: [id], onDelete: Cascade)

  @@unique([provider, provider_id])
  @@index([user_id])
}

// schema.prisma

model store {
  name                String
  slug                String                 @unique
  customDomain        String?                @unique
  logo                String?
  currency            String                 @default("USD")
  ownerId             BigInt
  createdAt           DateTime               @default(now())
  updatedAt           DateTime
  status              BigInt                 @default(1)
  id                  BigInt                 @id @default(autoincrement())
  description         String?
  menus               StoreMenu[]
  pages               StorePage[]
  theme               StoreTheme?
  sections            ThemeSection[]
  products            Product[]
  productTypes        ProductType[]
  tags                Tag[]
  collections         Collection[]
  orders              Order[]
  paymentAccounts     PaymentAccount[]
  checkouts           Checkout[]

  users users @relation(fields: [ownerId], references: [id])
}

model StoreTheme {
  id         BigInt   @id @default(autoincrement())
  store_id   BigInt   @unique
  menu_id    BigInt?
  store      store    @relation(fields: [store_id], references: [id])
  colors     Json     @default("{}")
  typography Json     @default("{}")
  header     Json     @default("{}")
  footer     Json     @default("{}")
  settings   Json?
  content    Json?
  created_at DateTime @default(now()) @db.Timestamp(6)
  updated_at DateTime @default(now()) @db.Timestamp(6)
}

model ThemeSection {
  id         BigInt   @id @default(autoincrement())
  store_id   BigInt
  store      store    @relation(fields: [store_id], references: [id], onDelete: Cascade)
  type       String
  name       String
  settings   Json     @default("{}")
  blocks     Json     @default("[]")
  sort_order Int      @default(0)
  is_active  Boolean  @default(true)
  page_type  String   @default("home")
  created_at DateTime @default(now())
  updated_at DateTime @updatedAt

  @@index([store_id, page_type, sort_order])
}

model StorePage {
  title      String
  slug       String
  type       PageType @default(STANDARD)
  content    String?
  image_url  String?
  is_active  Boolean  @default(true)
  sort_order Int      @default(0)
  created_at DateTime @default(now())
  updated_at DateTime @updatedAt
  id         BigInt   @id @default(autoincrement())
  store_id   BigInt
  store      store    @relation(fields: [store_id], references: [id])

  @@unique([store_id, slug])
}

model StoreMenu {
  name     String
  id       BigInt     @id @default(autoincrement())
  store_id BigInt
  handle   String     @unique
  items    MenuItem[]
  store    store      @relation(fields: [store_id], references: [id])
}

model MenuItem {
  title       String
  url         String
  sort_order  Int          @default(0)
  id          BigInt       @id @default(autoincrement())
  menu_id     BigInt
  parent_id   BigInt?
  resource_id BigInt?
  type        MenuItemType
  menu        StoreMenu    @relation(fields: [menu_id], references: [id])
  parent      MenuItem?    @relation("MenuTree", fields: [parent_id], references: [id])
  children    MenuItem[]   @relation("MenuTree")
}

enum PageType {
  STANDARD
  PRODUCT_WITH_HERO
  PRODUCT_WITHOUT_HERO
}

enum MenuItemType {
  HOME
  SEARCH
  COLLECTION
  PRODUCT
  PAGE
  BLOG
  POLICY
  ORDERS
  PROFILE
  CUSTOM
}

model StoreThemePublished {
  id       BigInt @id @default(autoincrement())
  store_id BigInt @unique

  theme    Json
  sections Json
  menus    Json

  created_at DateTime @default(now())
  updated_at DateTime @updatedAt
}

enum ProductStatus {
  DRAFT
  ACTIVE
  ARCHIVED
  UNLISTED
}

model Product {
  id BigInt @id @default(autoincrement())

  store_id BigInt

  title  String
  handle String

  description String?

  status ProductStatus @default(DRAFT)

  product_type_id BigInt?

  category String?

  charge_tax Boolean @default(true)

  seo_title String?
  seo_desc  String?

  created_at DateTime  @default(now())
  updated_at DateTime  @updatedAt
  deleted_at DateTime?

  store store @relation(fields: [store_id], references: [id], onDelete: Cascade)

  productType ProductType? @relation(fields: [product_type_id], references: [id])

  images      ProductImage[]
  variants    ProductVariant[]
  options     ProductOption[]
  tags        ProductTag[]
  collections ProductCollection[]

  @@unique([store_id, handle])
  @@index([store_id])
  @@index([status])
  @@index([product_type_id])
  @@index([deleted_at])
}

model ProductType {
  id       BigInt @id @default(autoincrement())
  store_id BigInt
  name     String

  created_at DateTime @default(now())
  updated_at DateTime @updatedAt

  store    store     @relation(fields: [store_id], references: [id], onDelete: Cascade)
  products Product[]

  @@unique([store_id, name])
  @@index([store_id])
}

model Tag {
  id       BigInt @id @default(autoincrement())
  store_id BigInt
  name     String

  created_at DateTime @default(now())
  updated_at DateTime @updatedAt

  store    store        @relation(fields: [store_id], references: [id], onDelete: Cascade)
  products ProductTag[]

  @@unique([store_id, name])
  @@index([store_id])
}

model ProductTag {
  product_id BigInt
  tag_id     BigInt

  product Product @relation(fields: [product_id], references: [id], onDelete: Cascade)
  tag     Tag     @relation(fields: [tag_id], references: [id], onDelete: Cascade)

  @@id([product_id, tag_id])
  @@index([tag_id])
}

model ProductImage {
  id BigInt @id @default(autoincrement())

  product_id BigInt

  url String
  key String? // ← جديد: مسار R2 للحذف
  alt String?

  position Int @default(0)

  created_at DateTime @default(now())

  product Product @relation(fields: [product_id], references: [id], onDelete: Cascade)

  @@index([product_id])
}

model ProductOption {
  id BigInt @id @default(autoincrement())

  product_id BigInt

  name String

  position Int @default(0)

  product Product @relation(fields: [product_id], references: [id], onDelete: Cascade)

  colors Json?

  display_type String?

  values ProductOptionValue[]

  @@index([product_id])
}

model ProductOptionValue {
  id BigInt @id @default(autoincrement())

  option_id BigInt

  value String

  option ProductOption @relation(fields: [option_id], references: [id], onDelete: Cascade)

  @@index([option_id])
}

model ProductVariant {
  id BigInt @id @default(autoincrement())

  product_id BigInt

  title String

  price            Decimal? @db.Decimal(10, 2)
  compare_at_price Decimal? @db.Decimal(10, 2)
  cost_per_item    Decimal? @db.Decimal(10, 2)

  sku     String?
  barcode String?

  inventory_qty Int @default(0)

  track_inventory  Boolean @default(true)
  continue_selling Boolean @default(false)

  option1 String?
  option2 String?
  option3 String?

  image_url String?
  image_key String?

  image_id BigInt?

  position Int @default(0)

  created_at DateTime @default(now())
  updated_at DateTime @updatedAt

  product Product @relation(fields: [product_id], references: [id], onDelete: Cascade)

  @@index([product_id])
  @@index([sku])
}

model Upload {
  id            BigInt   @id @default(autoincrement())
  key           String   @unique
  url           String
  mime_type     String
  size          Int
  store_id      BigInt
  status        String   @default("pending") // pending | attached
  attached_type String? // "product" | "variant"
  attached_id   BigInt?
  created_at    DateTime @default(now())

  @@index([status, created_at])
  @@index([store_id])
}

model Collection {
  id          Int     @id @default(autoincrement())
  name        String
  handle      String
  description String?
  image_url   String?
  image_key   String?

  seo_title       String?
  seo_description String?

  storeId BigInt

  store store @relation(fields: [storeId], references: [id], onDelete: Cascade)

  products ProductCollection[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([storeId, handle])
}

model ProductCollection {
  productId    BigInt
  collectionId Int
  position     Int    @default(0)

  product    Product    @relation(fields: [productId], references: [id], onDelete: Cascade)
  collection Collection @relation(fields: [collectionId], references: [id], onDelete: Cascade)

  @@id([productId, collectionId])
}

enum OrderStatus {
  PENDING // لسه جديد، محتاج التاجر يراجعه
  CONFIRMED // التاجر أكد الطلب
  PROCESSING // بيتجهز
  SHIPPED // اتشحن
  DELIVERED // وصل
  CANCELLED // اتلغى
}

// جاهزة لبوابات الدفع اللي هتضيفها بعدين — دلوقتي كل الطلبات هتتعمل
// بحالة UNPAID لحد ما تضيف منطق الدفع الفعلي.
enum PaymentStatus {
  UNPAID
  PAID
  REFUNDED
  FAILED
}

model Order {
  id BigInt @id @default(autoincrement())

  // العلاقات
  store_id BigInt
  store    store  @relation(fields: [store_id], references: [id], onDelete: Restrict, onUpdate: Cascade)

  // رقم الطلب الظاهر للعميل
  order_number String

  // حالة الطلب
  status OrderStatus @default(PENDING)

  /// عملة الطلب وقت إنشائه.
  /// ⚠️ متخزّنة على الطلب نفسه مش متقروءة من المتجر: لو التاجر غيّر
  /// عملة متجره، الطلبات القديمة لازم تفضل بعملتها الأصلية.
  currency String @default("USD") @db.VarChar(3)

  // ملخص سريع لحالة الدفع. المصدر التفصيلي هييجي في المرحلة 1b.
  payment_status PaymentStatus @default(UNPAID)

  // آخر وسيلة دفع استخدمها العميل.
  //
  // ⚠️ العمود ده في قاعدة البيانات TEXT مش enum. كان متكتب هنا كـ
  // PaymentProviderKey? بالغلط، وده كان بيخلي Prisma يقترح حذف العمود
  // وإعادة إنشائه — لأن Postgres مابيحوّلش text لـ enum في مكانه.
  //
  // اتصلّح هنا عشان الوصف يطابق الواقع. التحويل لـ enum بيحصل في
  // المرحلة 1b بهجرة صريحة فيها USING clause.
  payment_method String?

  /// وقت اكتمال الدفع فعلاً
  paid_at DateTime? @db.Timestamptz(6)

  /// الـ checkout اللي الطلب اتولد منه. null للطلبات القديمة.
  checkout_id BigInt? @unique

  // بيانات العميل
  customer_name  String
  customer_phone String
  customer_email String?

  // العنوان
  address_line String
  city         String
  notes        String?

  // الأسعار
  subtotal Decimal @db.Decimal(10, 2)
  total    Decimal @db.Decimal(10, 2)

  // المنتجات
  items OrderItem[]

  // التواريخ
  created_at DateTime @default(now())
  updated_at DateTime @updatedAt

  @@unique([store_id, order_number])
  @@index([store_id])
  @@index([store_id, status])
}

model OrderItem {
  id       BigInt @id @default(autoincrement())
  order_id BigInt
  order    Order  @relation(fields: [order_id], references: [id], onDelete: Cascade)

  // مش هنعمل relation إجباري على Product/ProductVariant عشان لو المنتج
  // اتحذف بعدين، تفاصيل الطلب القديم تفضل زي ما هي
  product_id BigInt?
  variant_id BigInt?

  title         String
  variant_title String?
  price         Decimal @db.Decimal(10, 2)
  qty           Int
  image_url     String?
}

enum PaymentProviderKey {
  cod
  bank_transfer
  paymob
  kashier
  stripe
  fawry
  paypal
  paytabs
  moyasar
  paylink
  tap
  tabby
  taager
  my_fatoorah
  fawaterk
  xpay
  ziina
  tamara
  easykash
  upay
  fabmisr
}

/// وسيلة الدفع زي ما العميل بيشوفها.
///
/// مختلفة عن PaymentProviderKey: البوابة الواحدة بتقدّم أكتر من وسيلة،
/// والوسيلة الواحدة بتتقدّم من أكتر من بوابة.
enum PaymentMethodKey {
  card
  mada
  knet
  benefit
  apple_pay
  google_pay
  wallet
  kiosk
  bank_transfer
  cod
  bnpl
}

enum OwnershipModel {
  /// التاجر بيدخل مفاتيح البوابة بنفسه
  merchant_credentials
  /// حساب متصل تديره المنصة — المرحلة 6
  platform_managed
}

enum PaymentAccountStatus {
  /// اتعمل بس لسه مافيش بيانات اعتماد
  draft
  /// بيانات الاعتماد موجودة والتحقق منها لسه ماتمش
  verifying
  active
  disabled
  /// آخر تحقق فشل — التفاصيل في last_error
  errored
}

/// نوع الالتزام اللي وسيلة الدفع بتنتجه لما العميل يأكّد الطلب
enum CommitmentKind {
  /// تفويض أو تحصيل نجح فعلاً (Stripe / Paymob / Moyasar / Tap)
  funds_secured
  /// وعد غير ممول والتاجر بيقبله (الدفع عند الاستلام / تحويل بنكي)
  promise_accepted
  /// صدر رقم مرجعي والعميل هيدفع بعدين (فوري / كشك)
  awaiting_offline_settlement
}

enum CaptureMode {
  automatic
  manual
}

/// وسائل الدفع اللي العميل بيشوفها عند الدفع.
///
/// صف لكل (حساب × وسيلة × إعداد البوابة). ده اللي بيحل مشكلة البوابات
/// اللي بتقدّم أكتر من تكامل تحت نفس الحساب — زي Paymob اللي بيديك
/// integration_id مختلف للكارت والمحفظة والكشك، والتاجر عايز يعرضهم
/// كأزرار منفصلة في صفحة الدفع.
model PaymentMethodOffering {
  id BigInt @id @default(autoincrement())

  account_id BigInt
  account    PaymentAccount @relation(fields: [account_id], references: [id], onDelete: Cascade)

  /// نفس مالك الحساب — متكرر عشان الاستعلامات تتقيّد من غير join
  store_id BigInt
  mode     Mode

  method PaymentMethodKey

  /// معرّف التكامل عند البوابة (مثال: integration_id في Paymob).
  /// نص فاضي معناه إن البوابة مالهاش تكاملات متعددة.
  gateway_method_config String @default("") @db.VarChar(255)

  enabled  Boolean @default(false)
  position Int     @default(0)

  /// الاسم اللي بيظهر للعميل. null معناه استخدم الاسم الافتراضي للوسيلة.
  display_name_ar String? @db.VarChar(120)
  display_name_en String? @db.VarChar(120)

  /// قيود اختيارية: أقل/أكبر مبلغ، عملات مسموحة، مدن مسموحة.
  /// المبالغ بالوحدات الصغرى زي أي مبلغ في المشروع.
  constraints Json?

  /// نوع الالتزام اللي الوسيلة دي بتنتجه — ده اللي بيخلي الدفع عند
  /// الاستلام وفوري و Stripe يمرّوا بنفس المسار في المرحلة 1b.2.
  commitment_kind CommitmentKind @default(funds_secured)

  capture_mode CaptureMode @default(automatic)

  created_at DateTime @default(now()) @db.Timestamptz(6)
  updated_at DateTime @updatedAt @db.Timestamptz(6)

  @@unique([account_id, method, gateway_method_config])
  @@index([store_id, mode])
  @@index([account_id])
  @@index([store_id, mode, enabled, position])
  @@map("payment_method_offerings")
}

/// اتصال متجر ببوابة دفع.
///
/// المتجر ممكن يكون عنده أكتر من حساب لنفس البوابة (عملات مختلفة،
/// كيانات مختلفة، أو ترحيل من حساب لحساب)، وعشان كده الاسم المعروض
/// جزء من مفتاح التفرّد.
///
/// ⚠️ المفتاح الأساسي لازم يفضل BigInt مربوط بـ sequence: بيانات
/// الاعتماد مشفّرة بـ AAD مربوط بـ id الصف، والـ id لازم يبقى معروف
/// قبل التشفير (شوف IdReservationService).
model PaymentAccount {
  id BigInt @id @default(autoincrement())

  store_id BigInt
  store    store  @relation(fields: [store_id], references: [id], onDelete: Cascade)

  /// عزل كامل بين بيانات الاختبار والبيانات الحقيقية
  mode Mode

  gateway PaymentProviderKey

  /// اسم من اختيار التاجر يفرّق بين حسابات نفس البوابة
  display_name String @db.VarChar(120)

  /// merchant_credentials = التاجر بيدخل مفاتيحه بنفسه (الوضع الحالي)
  /// platform_managed     = حساب متصل تديره المنصة (المرحلة 6)
  ownership_model OwnershipModel @default(merchant_credentials)

  /// معرّف الحساب المتصل عند البوابة — null في وضع مفاتيح التاجر
  connected_account_ref String? @db.VarChar(255)

  /// ═══ بيانات الاعتماد ═══
  ///
  /// مشفّرة بمفتاح مشتق للمتجر (StoreKeyService)، والـ AAD مربوط بـ:
  ///     (store_id, mode, 'payment_account', id, 'credentials')
  ///
  /// ⚠️ **مابترجعش أبداً في أي رد API.** الواجهة بتاخد تلميح مقنّع
  /// (credentials_hint) و is_configured بس.
  credentials_envelope String?
  credential_kek_version Int?
  credential_dek_version Int?

  /// HMAC بمفتاح المتجر — للكشف عن التغيير ولعرض تلميح مقنّع،
  /// مش hash عادي للسر عشان مايبقاش قابل للتخمين
  credentials_fingerprint String? @db.VarChar(64)

  /// آخر 4 حروف من كل حقل، للعرض بس. مفيش أسرار كاملة هنا.
  credentials_hint Json?

  /// العملة اللي البوابة بتسوّي بيها على الحساب ده
  settlement_currency String? @db.VarChar(3)

  status PaymentAccountStatus @default(draft)

  /// نتيجة آخر تحقق من بيانات الاعتماد — بيتملّى في المرحلة 1b.2
  /// لما أول أدابتر يقدر يكلّم البوابة فعلاً
  last_verified_at DateTime? @db.Timestamptz(6)
  last_error       String?

  offerings PaymentMethodOffering[]

  created_at DateTime @default(now()) @db.Timestamptz(6)
  updated_at DateTime @updatedAt @db.Timestamptz(6)

  @@unique([store_id, mode, gateway, display_name])
  @@index([store_id, mode])
  @@index([store_id, mode, status])
  @@map("payment_accounts")
}


// ═══════════════════════════════════════════════════════════════════
// Phase 1a — Foundations
//
// إضافات فقط. مفيش أي جدول موجود في قاعدة البيانات بيتغيّر.
//
// الموديلات المؤقتة اللي كانت مكتوبة هنا لجداول دفع **مش موجودة**
// في قاعدة البيانات اتشالت من الملف ده، لأن وجودها كان بيخلي هجرة
// المرحلة 1a تحاول تنشئ جداول مش من نطاقها. اتحفظت كاملة في:
//
//     prisma/phase1b-placeholders.prisma.bak
//
// وهترجع بشكلها النهائي في المرحلة 1b.
//
// PaymentProviderKey اتساب زي ما هو — الـ enum موجود في قاعدة البيانات
// وبيستخدمه PaymentAccount.
//
// ملاحظة على التقسيم (partitioning): الجداول دي مش مقسّمة في 1a لأنها
// هتفضل فاضية في الإنتاج (مفيش producer). التقسيم بيتعمل في 1b مع أول
// producer، وقتها التحويل drop-and-recreate على جدول فاضي.
// ═══════════════════════════════════════════════════════════════════

/// وضع التشغيل — العزل الكامل بين بيانات الاختبار والبيانات الحقيقية.
/// في المرحلة 1a بيتبعت كمعامل صريح؛ الحل التلقائي بييجي في 1b.
enum Mode {
  test
  live
}

enum OutboxStatus {
  pending
  claimed
  published
  failed
  dead
}

enum IdempotencyStatus {
  in_flight
  completed
  failed
}

/// سجل الطلبات المتكرّرة (Idempotency).
///
/// مفيش FK على store عن قصد: السجل لازم يفضل موجود حتى لو المتجر
/// اتحذف، عشان التدقيق. store_id هنا قيمة نطاق مش اعتماد مرجعي.
model PaymentIdempotencyRecord {
  id BigInt @id @default(autoincrement())

  store_id BigInt
  mode     Mode

  /// اسم العملية المنطقي، مثال: "payments.create_intent"
  scope String @db.VarChar(100)

  /// المفتاح اللي بعته العميل في هيدر Idempotency-Key
  idempotency_key String @db.VarChar(255)

  /// hash للـ method + path + body بعد التطبيع — عشان نكشف
  /// نفس المفتاح مع body مختلف
  request_fingerprint String @db.VarChar(64)

  status IdempotencyStatus @default(in_flight)

  response_status_code Int?
  /// الرد المخزّن للإعادة. بيمرّ على redaction قبل التخزين.
  response_body        Json?

  /// حجز للطلب الجاري — الطلب المكرر المتزامن بيستنى أو يترفض
  locked_until DateTime? @db.Timestamptz(6)

  created_at   DateTime  @default(now()) @db.Timestamptz(6)
  completed_at DateTime? @db.Timestamptz(6)
  expires_at   DateTime  @db.Timestamptz(6)

  @@unique([store_id, mode, scope, idempotency_key])
  @@index([expires_at])
  @@index([store_id, mode])
  @@map("payment_idempotency_records")
}

/// صندوق الصادر (Transactional Outbox).
///
/// الرسالة بتتكتب جوه نفس الـ transaction بتاعة تغيير الحالة، فمفيش
/// احتمال إن الحالة تتحفظ والحدث يضيع أو العكس.
///
/// مفيش FK على store: الحذف المتتالي ماينفعش يمسح أحداث لسه ماتسلّمتش.
model OutboxMessage {
  id BigInt @id @default(autoincrement())

  store_id BigInt
  mode     Mode

  /// نوع الكيان المصدر، مثال: "checkout"
  aggregate_type String @db.VarChar(64)
  aggregate_id   String @db.VarChar(64)

  /// نوع الحدث، مثال: "checkout.committed"
  event_type    String @db.VarChar(100)
  /// إصدار عقد الحدث — موجود من الرسالة رقم 1، مش من رقم 100000
  event_version Int    @default(1)

  payload Json

  status OutboxStatus @default(pending)

  /// حجز للتوزيع — بيخلي أكتر من instance تشتغل بأمان من غير Redis
  claimed_by        String?   @db.VarChar(100)
  claim_expires_at  DateTime? @db.Timestamptz(6)

  attempts        Int       @default(0)
  next_attempt_at DateTime  @default(now()) @db.Timestamptz(6)
  last_error      String?

  /// وقت حدوث الحدث في العمل (مقابل وقت تسجيله في النظام)
  occurred_at  DateTime  @db.Timestamptz(6)
  created_at   DateTime  @default(now()) @db.Timestamptz(6)
  published_at DateTime? @db.Timestamptz(6)

  consumptions ConsumedEvent[]

  @@index([status, next_attempt_at])
  @@index([aggregate_type, aggregate_id])
  @@index([store_id, mode])
  @@index([created_at])
  @@map("outbox_messages")
}

/// سجل استهلاك الأحداث لكل مستهلك.
///
/// القيد الفريد ده **هو** ضمانة الـ at-least-once، مش فحص في الكود.
model ConsumedEvent {
  id BigInt @id @default(autoincrement())

  consumer_name String @db.VarChar(100)
  message_id    BigInt

  store_id BigInt
  mode     Mode

  consumed_at DateTime @default(now()) @db.Timestamptz(6)
  /// نتيجة مختصرة للتشخيص، مثال: "ok" أو "skipped:duplicate"
  result      String?  @db.VarChar(255)

  message OutboxMessage @relation(fields: [message_id], references: [id], onDelete: Cascade)

  @@unique([consumer_name, message_id])
  @@index([message_id])
  @@index([store_id, mode])
  @@map("consumed_events")
}

// ═══════════════════════════════════════════════════════════════════
// المرحلة 1b.2 — Checkout والنوايا والدفتر
//
// المبادئ اللي الجداول دي مبنية عليها:
//   • المبالغ أعداد صحيحة بالوحدات الصغرى (amount_minor) + عملة.
//   • الطلب بيتولد من **التزام** الـ checkout، مش قبله.
//   • الحالة بتتحرس بجدول انتقالات في الكود، مابتتكتبش فوقها.
//   • كل حركة مالية قيد غير قابل للتعديل في الدفتر؛ الأرصدة تُشتق.
//   • مفيش FK على store لأي سجل مالي: حذف المتجر ماينفعش يمسح
//     سجلات مالية. الـ FK موجود بس على الكيانات المؤقتة (Checkout).
// ═══════════════════════════════════════════════════════════════════

enum CheckoutStatus {
  open
  pending_payment
  committed
  expired
  abandoned
  failed
}

enum QuoteComponentKind {
  line_subtotal
  shipping
  tax
  discount
  payment_fee
  adjustment
}

enum ReservationState {
  held
  converted
  released
  expired
}

enum PaymentIntentContextKind {
  checkout
  order_balance
  subscription_cycle
  invoice
  manual
}

enum PaymentIntentUsage {
  one_time
  setup
  recurring
}

enum PaymentIntentStatus {
  created
  requires_payment_method
  requires_action
  processing
  authorized
  partially_captured
  captured
  partially_refunded
  refunded
  failed
  cancelled
  expired
}

enum PaymentAttemptStatus {
  initialized
  requires_action
  processing
  authorized
  succeeded
  failed
  expired
  cancelled
}

/// الخطوة اللي العميل لازم يعملها عشان الدفع يكمل.
/// reference_code و bank_instructions هما اللي بيخلّوا فوري والتحويل
/// البنكي يمرّوا بنفس المسار بتاع البوابات الأونلاين.
enum NextActionKind {
  none
  redirect
  iframe
  client_sdk
  reference_code
  bank_instructions
  poll
}

enum CaptureStatus {
  pending
  succeeded
  failed
}

enum AllocationKind {
  revenue
  platform_fee
  vendor_share
}

enum BeneficiaryKind {
  store
  vendor
  platform
}

enum LedgerAccountType {
  psp_receivable
  offline_receivable
  cash_collected
  settled_out
  sales_revenue
  refunds_contra
  psp_fee_expense
  platform_fee_expense
  platform_fee_payable
  disputes_held
  chargeback_loss
  fx_conversion
  suspense
}

enum PostingDirection {
  debit
  credit
}

enum PaymentEventSource {
  api
  webhook
  reconciliation
  return_url
  merchant
  system
}

/// سلة مجمّدة + عنوان + سعر، قبل ما تتحوّل لطلب.
///
/// الـ checkout بياخد token معتّم مش رقم تسلسلي: أرقام الطلبات
/// بتتحجز عند الالتزام بس، فالسلة المهجورة مابتستهلكش رقم.
model Checkout {
  id BigInt @id @default(autoincrement())

  store_id BigInt
  store    store  @relation(fields: [store_id], references: [id], onDelete: Cascade)
  mode     Mode

  /// المعرّف العلني اللي بيظهر في رابط صفحة الدفع
  token String @unique @db.VarChar(64)

  status CheckoutStatus @default(open)

  customer_name  String? @db.VarChar(160)
  customer_email String? @db.VarChar(160)
  customer_phone String? @db.VarChar(40)

  shipping_address Json?

  /// عملة العرض، مجمّدة وقت الإنشاء
  currency String @db.VarChar(3)

  /// مجموع كل مكوّنات السعر، بالوحدات الصغرى
  quote_total_minor BigInt @default(0)

  /// ختم على السلة والسعر — بيمنع تعديل السلة أثناء الدفع
  quote_hash String? @db.VarChar(64)

  /// وسيلة الدفع اللي العميل اختارها
  selected_offering_id BigInt?

  expires_at   DateTime  @db.Timestamptz(6)
  committed_at DateTime? @db.Timestamptz(6)

  /// الطلب اللي اتولد من الالتزام
  order_id BigInt? @unique

  client_ip  String? @db.VarChar(64)
  user_agent String? @db.VarChar(400)

  items        CheckoutLineItem[]
  components   QuoteComponent[]
  reservations InventoryReservation[]

  created_at DateTime @default(now()) @db.Timestamptz(6)
  updated_at DateTime @updatedAt @db.Timestamptz(6)

  @@index([store_id, mode, status])
  @@index([expires_at])
  @@index([store_id, mode, created_at])
  @@map("checkouts")
}

/// لقطة من المنتج وقت الإضافة للسلة.
/// المراجع nullable عن قصد: الطلب لازم يفضل مقروء حتى لو المنتج اتحذف.
model CheckoutLineItem {
  id BigInt @id @default(autoincrement())

  checkout_id BigInt
  checkout    Checkout @relation(fields: [checkout_id], references: [id], onDelete: Cascade)

  product_id BigInt?
  variant_id BigInt?

  title         String  @db.VarChar(255)
  variant_title String? @db.VarChar(255)
  image_url     String? @db.VarChar(600)

  unit_price_minor BigInt
  quantity         Int

  created_at DateTime @default(now()) @db.Timestamptz(6)

  @@index([checkout_id])
  @@map("checkout_line_items")
}

/// مكوّن من مكوّنات السعر.
///
/// الشحن والضرايب والخصومات بتتحسب في وحدات منفصلة وبتضيف صفوف هنا،
/// فـ Checkout مش محتاج يعرف حاجة عنهم.
/// ثابت: quote_total_minor = مجموع amount_minor لكل المكوّنات.
model QuoteComponent {
  id BigInt @id @default(autoincrement())

  checkout_id BigInt
  checkout    Checkout @relation(fields: [checkout_id], references: [id], onDelete: Cascade)

  kind  QuoteComponentKind
  label String             @db.VarChar(160)

  /// ممكن يكون سالب (خصم)
  amount_minor BigInt

  /// الوحدة اللي حسبت المكوّن ده
  source_ref String? @db.VarChar(120)

  position Int @default(0)

  @@index([checkout_id])
  @@map("quote_components")
}

/// حجز مخزون مؤقت أثناء الدفع.
///
/// بيتحوّل لسطر طلب عند الالتزام، وبيترجع عند الانتهاء أو الفشل.
model InventoryReservation {
  id BigInt @id @default(autoincrement())

  checkout_id BigInt
  checkout    Checkout @relation(fields: [checkout_id], references: [id], onDelete: Cascade)

  store_id   BigInt
  mode       Mode
  variant_id BigInt
  quantity   Int

  state ReservationState @default(held)

  expires_at DateTime  @db.Timestamptz(6)
  settled_at DateTime? @db.Timestamptz(6)

  created_at DateTime @default(now()) @db.Timestamptz(6)

  @@unique([checkout_id, variant_id])
  @@index([state, expires_at])
  @@index([store_id, mode, variant_id])
  @@map("inventory_reservations")
}

/// جذر عملية الدفع.
///
/// بيفضل موجود عبر المحاولات الفاشلة: العميل اللي بطاقته اترفضت وجرّب
/// تانية بيبقى عنده نية واحدة ومحاولتين، مش عمليتين منفصلتين.
///
/// context_kind بيفك الارتباط بالـ checkout عشان الاشتراكات والفواتير
/// في المراحل الجاية تقدر تعمل نوايا من غير سلة.
model PaymentIntent {
  id BigInt @id @default(autoincrement())

  store_id BigInt
  mode     Mode

  context_kind PaymentIntentContextKind @default(checkout)
  context_id   String                   @db.VarChar(64)

  amount_minor BigInt
  currency     String @db.VarChar(3)

  capture_method CaptureMode        @default(automatic)
  usage          PaymentIntentUsage @default(one_time)

  status PaymentIntentStatus @default(created)

  /// نسخ مخزّنة من حقيقة الدفتر — بيتأكد منها فاحص الثوابت
  authorized_total_minor BigInt @default(0)
  captured_total_minor   BigInt @default(0)
  refunded_total_minor   BigInt @default(0)

  /// تحكّم تفاؤلي في التزامن
  version Int @default(0)

  /// مفتاح العميل — فريد لكل متجر ووضع
  idempotency_key String? @db.VarChar(255)

  account_id  BigInt?
  offering_id BigInt?

  expires_at  DateTime? @db.Timestamptz(6)
  terminal_at DateTime? @db.Timestamptz(6)

  metadata Json?

  attempts PaymentAttempt[]
  captures Capture[]
  events   PaymentEvent[]

  created_at DateTime @default(now()) @db.Timestamptz(6)
  updated_at DateTime @updatedAt @db.Timestamptz(6)

  @@unique([store_id, mode, idempotency_key])
  @@index([store_id, mode, status])
  @@index([context_kind, context_id])
  @@index([status, expires_at])
  @@index([store_id, mode, created_at])
  @@map("payment_intents")
}

/// محاولة واحدة على حساب بوابة واحد.
///
/// بتحمل حقائق التفويض بنفسها — مفيش كيان Authorization منفصل، لأن
/// مفيش بوابة بتدي أكتر من تفويض للمحاولة الواحدة.
model PaymentAttempt {
  id BigInt @id @default(autoincrement())

  intent_id BigInt
  intent    PaymentIntent @relation(fields: [intent_id], references: [id], onDelete: Cascade)

  store_id BigInt
  mode     Mode

  /// ترتيب المحاولة داخل النية
  sequence Int

  account_id  BigInt?
  offering_id BigInt?

  status PaymentAttemptStatus @default(initialized)

  authorized_amount_minor   BigInt?
  authorization_expires_at  DateTime? @db.Timestamptz(6)

  /// معرّفات البوابة — التفرّد على مستوى الحساب مش البوابة، عشان
  /// متجرين بنفس حساب البوابة مايتعارضوش
  gateway_reference  String? @db.VarChar(255)
  gateway_payment_id String? @db.VarChar(255)

  next_action_kind       NextActionKind @default(none)
  next_action_payload    Json?
  next_action_expires_at DateTime?      @db.Timestamptz(6)

  /// مشتق من (store, intent, sequence, operation) — مش عشوائي، عشان
  /// إعادة المحاولة تبعت نفس المفتاح للبوابة
  psp_idempotency_key String? @db.VarChar(255)

  /// من تصنيف الأخطاء الموحّد، مش نص البوابة الخام
  error_code        String? @db.VarChar(60)
  error_message_raw String?

  request_snapshot  Json?
  response_snapshot Json?

  created_at DateTime @default(now()) @db.Timestamptz(6)
  updated_at DateTime @updatedAt @db.Timestamptz(6)

  @@unique([intent_id, sequence])
  @@unique([account_id, gateway_reference])
  @@index([store_id, mode, status])
  @@index([gateway_reference])
  @@map("payment_attempts")
}

/// تحصيل فعلي لمبلغ.
/// أكتر من تحصيل للمحاولة الواحدة مسموح (شحن على دفعات).
model Capture {
  id BigInt @id @default(autoincrement())

  intent_id  BigInt
  intent     PaymentIntent @relation(fields: [intent_id], references: [id], onDelete: Cascade)
  attempt_id BigInt?

  store_id BigInt
  mode     Mode

  amount_minor BigInt
  currency     String @db.VarChar(3)

  status CaptureStatus @default(pending)

  gateway_capture_ref String? @db.VarChar(255)

  captured_at DateTime? @db.Timestamptz(6)

  allocations CaptureAllocation[]

  created_at DateTime @default(now()) @db.Timestamptz(6)
  updated_at DateTime @updatedAt @db.Timestamptz(6)

  @@index([intent_id, status])
  @@index([store_id, mode, status])
  @@map("captures")
}

/// توزيع التحصيل على المستفيدين.
///
/// في المرحلة دي صف واحد بس للمتجر نفسه (+ عمولة منصة اختيارية)،
/// لكن البُعد موجود من دلوقتي عشان السوق متعدد البائعين مايبقاش
/// إعادة كتابة للدفتر بعدين.
/// ثابت: مجموع amount_minor = amount_minor بتاع التحصيل.
model CaptureAllocation {
  id BigInt @id @default(autoincrement())

  capture_id BigInt
  capture    Capture @relation(fields: [capture_id], references: [id], onDelete: Cascade)

  beneficiary_id BigInt
  store_id       BigInt
  mode           Mode

  amount_minor BigInt
  kind         AllocationKind @default(revenue)

  @@index([capture_id])
  @@index([beneficiary_id])
  @@map("capture_allocations")
}

/// جهة تُنسب لها الإيرادات. مش حساب أموال — التاجر هو Merchant of Record.
model Beneficiary {
  id BigInt @id @default(autoincrement())

  store_id BigInt
  mode     Mode

  kind         BeneficiaryKind
  external_ref String?         @db.VarChar(120)

  default_currency String @db.VarChar(3)

  created_at DateTime @default(now()) @db.Timestamptz(6)

  @@unique([store_id, mode, kind, external_ref])
  @@index([store_id, mode])
  @@map("beneficiaries")
}

/// حساب في الدفتر.
///
/// ⚠️ العملة جزء من هوية الحساب: دفتر بيخلط عملات في رصيد واحد
/// مابينفعش يتجمع، وأي استعلام رصيد بيرجّع كلام فاضي.
model LedgerAccount {
  id BigInt @id @default(autoincrement())

  store_id BigInt
  mode     Mode
  currency String @db.VarChar(3)

  account_type LedgerAccountType

  beneficiary_id     BigInt?
  payment_account_id BigInt?

  postings LedgerPosting[]

  created_at DateTime @default(now()) @db.Timestamptz(6)

  /// الاسم صريح: الاسم التلقائي بيطلع 89 حرف، وPostgres بيقصّه عند 63
  @@unique([store_id, mode, currency, account_type, beneficiary_id, payment_account_id], map: "ledger_accounts_identity_key")
  @@index([store_id, mode, currency])
  @@map("ledger_accounts")
}

/// قيد محاسبي. غير قابل للتعديل — التصحيح بقيد عكسي مش بتحديث.
/// ثابت: مجموع المدين = مجموع الدائن، داخل عملة واحدة.
model JournalEntry {
  id BigInt @id @default(autoincrement())

  store_id BigInt
  mode     Mode
  currency String @db.VarChar(3)

  /// نوع القاعدة اللي أنتجت القيد. نص مش enum: التصنيف مفتوح
  /// وبيكبر مع كل بوابة جديدة.
  entry_type String @db.VarChar(60)

  source_kind String @db.VarChar(40)
  source_id   String @db.VarChar(64)

  /// مشتق من المحتوى — بيخلي الترحيل idempotent مهما كان مصدر الحدث
  dedupe_key String @unique @db.VarChar(120)

  occurred_at DateTime @db.Timestamptz(6)
  posted_at   DateTime @default(now()) @db.Timestamptz(6)

  reverses_entry_id BigInt?

  memo String? @db.VarChar(400)

  postings LedgerPosting[]

  @@index([store_id, mode, currency, occurred_at])
  @@index([source_kind, source_id])
  @@map("journal_entries")
}

model LedgerPosting {
  id BigInt @id @default(autoincrement())

  entry_id BigInt
  entry    JournalEntry @relation(fields: [entry_id], references: [id], onDelete: Cascade)

  ledger_account_id BigInt
  ledgerAccount     LedgerAccount @relation(fields: [ledger_account_id], references: [id], onDelete: Restrict)

  direction PostingDirection

  /// دايماً موجب — الاتجاه في direction
  amount_minor BigInt

  @@index([entry_id])
  @@index([ledger_account_id])
  @@map("ledger_postings")
}

/// سجل ملحق غير قابل للتعديل لكل ما يحصل للنية.
/// للتدقيق وأدلة النزاعات — مش لإعادة بناء الحالة وقت التشغيل.
model PaymentEvent {
  id BigInt @id @default(autoincrement())

  intent_id BigInt
  intent    PaymentIntent @relation(fields: [intent_id], references: [id], onDelete: Cascade)

  store_id BigInt
  mode     Mode

  /// من التصنيف الموحّد للأحداث
  event_type String @db.VarChar(60)

  /// مشتق من المحتوى مش من وسيلة النقل — نفس الحقيقة من webhook أو
  /// مطابقة أو رجوع العميل بتدي نفس المفتاح
  dedupe_key String @unique @db.VarChar(120)

  source PaymentEventSource

  /// false معناه الحدث اتسجّل لكن ماغيّرش الحالة (قديم أو مكرر)
  applied           Boolean @default(true)
  superseded_reason String? @db.VarChar(120)

  payload_redacted Json?

  occurred_at DateTime? @db.Timestamptz(6)
  recorded_at DateTime  @default(now()) @db.Timestamptz(6)

  @@index([intent_id, recorded_at])
  @@index([store_id, mode, event_type])
  @@map("payment_events")
}
